{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "venv\\Scripts\\activate && python server.py",
    "install": "pip install virtualenv && virtualenv venv && venv\\Scripts\\activate && pip install -r requirements.txt"
  },
  "license": "MIT"
}
